export enum TypeEventEnum {
  EVENT = 'event',
  TASK = 'task',
}

export enum StatusEventEnum {
  UPDATED = 'updated',
  CREATED = 'created',
  DELETED = 'deleted',
}

export enum StatusEnum {
  ERROR = 'error',
  SUCCESS = 'success',
}

export enum SenderProviderEventEnum {
  OUTLOOK = 'outlook',
  CLICK_UP = 'click_up',
}
